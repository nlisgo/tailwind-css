# Tailwind CSS Showcase

A comprehensive showcase of Tailwind CSS features with mobile-first responsive design and custom CSS examples.

## Features

- 📱 Mobile-first responsive design
- 🎨 Tailwind CSS v4 utilities
- ✨ Custom CSS animations and components
- 🎯 Breakpoint demonstrations
- 📦 Component examples (cards, forms, alerts, badges)
- 🌈 Gradient effects and glass morphism

## Local Development

### Prerequisites

- Node.js 20 or higher
- npm

### Setup

1. Clone the repository
```bash
git clone <your-repo-url>
cd tailwind-css
```

2. Install dependencies
```bash
npm install
```

3. Build CSS (one-time build)
```bash
npm run build
```

4. Watch for changes during development (in a separate terminal)
```bash
npm run dev
```

5. Serve the site locally
```bash
# Using Python
python3 -m http.server 8000

# Then visit http://localhost:8000/src/index.html
```

Or simply open `src/index.html` directly in your browser (CSS will still load).

## Project Structure

```
.
├── src/
│   ├── index.html          # Main HTML file
│   ├── css/
│   │   └── style.css       # Source CSS with Tailwind import and custom styles
│   └── dist/
│       └── style.css       # Compiled CSS (generated by build)
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions workflow
└── package.json
```

## GitHub Pages Deployment

This project is configured to automatically deploy to GitHub Pages using GitHub Actions.

### Setup GitHub Pages

1. **Push your code to GitHub**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push -u origin main
```

2. **Enable GitHub Pages**
   - Go to your repository on GitHub
   - Navigate to **Settings** → **Pages**
   - Under "Build and deployment":
     - **Source**: Select "GitHub Actions"
   - Save the settings

3. **Trigger Deployment**
   - The workflow automatically runs on every push to `main` or `master`
   - Or manually trigger it:
     - Go to **Actions** tab
     - Select "Build and Deploy to GitHub Pages"
     - Click "Run workflow"

4. **Access Your Site**
   - After deployment completes (1-2 minutes)
   - Your site will be available at: `https://<username>.github.io/<repository-name>/`
   - Example: `https://nlisgo.github.io/tailwind-css/`

### Workflow Details

The GitHub Actions workflow (`.github/workflows/deploy.yml`):
1. Checks out the code
2. Sets up Node.js 20
3. Installs dependencies
4. Runs `npm run build` to compile Tailwind CSS
5. Copies `src/index.html` to root and `dist/` folder
6. Deploys to GitHub Pages with index.html at the root URL

### Troubleshooting

**If the site doesn't load:**
- Check the Actions tab for build errors
- Ensure GitHub Pages is set to use "GitHub Actions" as the source
- Visit the root URL (no need to add `/index.html`)

**If styles aren't loading:**
- The workflow builds the CSS automatically
- Check that `dist/style.css` is being created in the workflow
- Verify the HTML link path: `<link rel="stylesheet" href="dist/style.css">`

## Extending Tailwind v4 Theme

You can extend Tailwind's default theme using the `@theme` directive in `src/css/style.css`:

```css
@theme {
  --color-brand: #abc123;
  --spacing-72: 18rem;
  --animate-wiggle: wiggle 1s ease-in-out infinite;
}
```

Examples included:
- **Custom colors**: `bg-brand`, `bg-brand-light`, `bg-brand-dark`, `bg-accent`
- **Custom fonts**: `font-handwriting`, `font-display`
- **Custom animations**: `animate-spin-slow`, `animate-bounce-slow`, `animate-wiggle`
- **Custom spacing**: `p-72`, `p-84`, `p-96`, `p-128`
- **Custom border radius**: `rounded-4xl`, `rounded-5xl`

After modifying `@theme`, run `npm run build` to regenerate the CSS.

**Note:** Tailwind v4 uses `@theme` in CSS instead of `tailwind.config.js` for theme extensions.

## Custom CSS

You can also add your own custom CSS in `src/css/style.css` after the `@import "tailwindcss";` line.

Examples included:
- Custom animations (float, pulse, gradient shift)
- Custom component classes (cards, gradient text, glass effects)
- Custom utility classes (text shadows, scrollbar styling)
- CSS variables for reusable values

## Available Scripts

- `npm run dev` - Watch mode: rebuilds CSS on file changes
- `npm run build` - Production build: compiles and minifies CSS

## Technologies

- [Tailwind CSS v4](https://tailwindcss.com/)
- GitHub Actions for CI/CD
- GitHub Pages for hosting

## License

ISC
